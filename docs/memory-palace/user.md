---
name: User profile — ezyang
description: ezyang is a PyTorch core maintainer building mergedog as a personal harness; has direct write access to pytorch/pytorch.
type: user
originSessionId: 99555959-243d-47e3-a3cf-e0d88b509da7
---
Sole user/author. PyTorch core maintainer, **direct write access to
pytorch/pytorch**. Personal harness, not a multi-tenant tool.

- "Allow edits by maintainers" is moot when the PR head ref is in
  pytorch/pytorch — don't HALT.
- Treats prompt injection, idempotency, and Ctrl-C robustness as
  first-class, not bolt-ons.
- State at `~/.mergedog/`, repo at `~/Nursery/mergedog/`.
- Works from multiple machines; transcripts are machine-local (see
  `_meta_log.md`).
